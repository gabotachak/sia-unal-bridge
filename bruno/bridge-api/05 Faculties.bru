meta {
  name: 05 Faculties
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/faculties?campus={{campus}}
  body: none
  auth: none
}

params:query {
  campus: {{campus}}
}

assert {
  res.status: eq 200
}

docs {
  Fase 1 solo resuelve Bogotá (1101). Dispara cascada en vivo contra el SIA
  si no está cacheado — puede tardar unos segundos.
}
